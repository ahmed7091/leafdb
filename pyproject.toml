
[project]
name = "leafdb"
version = "0.1.0"
dependencies = ["click"]
requires-python = ">=3.8"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

